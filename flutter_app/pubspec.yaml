name: smart_attendance
description: Smart Attendance SaaS - Flutter Client
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  flutter_blue_plus: ^1.32.0
  crypto: ^3.0.3
  device_info_plus: ^10.1.0
  google_mlkit_face_detection: ^0.11.0
  flutter_local_notifications: ^17.2.1
  camera: ^0.11.0
  shared_preferences: ^2.2.3
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
